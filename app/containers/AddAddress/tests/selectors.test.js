// import { fromJS } from 'immutable';
// import { makeSelectAddAddressDomain } from '../selectors';

// const selector = makeSelectAddAddressDomain();

describe('makeSelectAddAddressDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false)
  })
})
