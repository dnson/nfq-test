// import { fromJS } from 'immutable';
// import { makeSelectAddressDetailDomain } from '../selectors';

// const selector = makeSelectAddressDetailDomain();

describe('makeSelectAddressDetailDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false)
  })
})
